[project]
name = "homelab-common"
version = "0.1.0"
description = "Shared utilities for homelab-assistant"
requires-python = ">=3.12"
dependencies = [
    "pydantic-settings>=2.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["homelab_common"]
